package Item;

public class FireSpell extends Spell{

    FireSpell(String type, String name, int cost, int level, int damage, int mana_cost) {
        super(type, name, cost, level, damage, mana_cost);
    }
}
